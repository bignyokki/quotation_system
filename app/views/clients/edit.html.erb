<p>作成日時:<%= @client.created_at %></p>
<p>最終更新:<%= @client.updated_at %></p>

<%= render partial: "form", locals: { client: @client } %>
<%= link_to '削除', client_path(@client.id), method: :delete, class: "btn btn--orange btn--radius" %>
<br>
<%= link_to "戻る", clients_path %>